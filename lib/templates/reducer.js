// Actions
import Actions from 'constants/Actions';

export const initialState = {};

export default function {reducerName}(previousState = initialState, action) {
	switch (action.type) {
		// case Actions.SOME_DUMMY_ACTION:
		//     return action.payload.someDummyPayload;

		default:
			return previousState;
	}
}
